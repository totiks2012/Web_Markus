# Web_Markus
WebMarkus is an ultra-lightweight Markdown editor with live preview, packaged into a single HTML file.
# WebMarkus 0.26 (Stable)

**WebMarkus** — это ультра-легковесный редактор Markdown с живым предпросмотром, упакованный в **один HTML-файл**.

Никаких серверов. Никаких зависимостей. Никакого Node.js.
Просто откройте файл в браузере и начните работать.

## 🚀 Философия

Проект создан как антитеза тяжеловесным редакторам на Electron. WebMarkus загружается мгновенно, работает полностью офлайн и сохраняет ваш прогресс локально. Это цифровой швейцарский нож для быстрого редактирования документации, заметок или статей.

## ⚡ Ключевые особенности

*   **Zero-Dependency:** Всё, что нужно, находится внутри одного файла `WebMarcus-026.html`.
*   **Instant Start:** Не требует установки. Работает на любой ОС, где есть браузер.
*   **Live Preview:** Синхронный скроллинг и мгновенный рендеринг Markdown в HTML.
*   **Print-Ready:** Специализированные CSS-стили для печати (`Ctrl+Alt+P`). При печати интерфейс скрывается, остаётся только чистый документ. Идеально для генерации PDF.
*   **Темы оформления:** Встроенный переключатель Dark/Light mode для комфортной работы ночью.
*   **Локальное сохранение:** Ваш текст автоматически сохраняется в `localStorage`. Вы не потеряете данные при случайном закрытии вкладки.

## 🛠 Технические детали

*   **Стек:** Vanilla JS + CSS3 + HTML5.
*   **Поиск и замена:** Встроенный инструмент (`Ctrl+Alt+F`) с подсветкой вхождений.
*   **Экспорт:**
    *   В Markdown (`.md`)
    *   В чистый HTML (для публикации)
    *   В PDF (через системный диалог печати)

## ⌨️ Горячие клавиши

| Сочетание | Действие |
| :--- | :--- |
| `Ctrl + Alt + F` | Открыть поиск/замену | -- важная ремарка в некоторых системах эти горячие клавишы могут быть заняты,
 напимер как эта файловым менеджером, все горячии клавиши захардкожены в скрипте, 
и для упрошения я не стал делать отдельного менеджера горячих клавиш,в угоду минимализма, просто перназначьте как я сделал
 для вызова файлового менеджера вместо ctrl+alt+f назначил alt+f

| `Ctrl + Alt + P` | Печать / Экспорт в PDF |
| `Alt + S` | Сохранить как .md |
| `Alt + O` | Открыть файл |
| `Alt + B/I/U` | Жирный / Курсив / Подчёркнутый |

## Как использовать

1.  Скачайте файл `WebMarcus-026.html`.
2.  Дважды кликните по нему.
3.  Вы великолепны.
